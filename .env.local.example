# Database Configuration
# For local development with Docker PostgreSQL, use localhost
DATABASE_URL=postgresql://rootpg:123456@localhost:5432/asset_management

# Alternative: Use individual variables (comment out DATABASE_URL if using these)
# POSTGRES_HOST=localhost
# POSTGRES_USER=rootpg
# POSTGRES_PASSWORD=123456
# POSTGRES_DB=asset_management
# POSTGRES_PORT=5432

# PostgreSQL SSL (usually false for local development)
POSTGRES_SSL=false

# Connection Pool Settings
PGPOOL_MAX=10
PGPOOL_IDLE=30000
PGPOOL_CONNECT_TIMEOUT=10000

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3091
NEXTAUTH_SECRET=your-secret-key-here-change-in-production

# Other environment variables...
